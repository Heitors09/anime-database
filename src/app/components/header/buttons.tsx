import { Button } from "@/components/ui/button"

const Buttons = () => {
  return (
    <div className="flex gap-4">
      <Button className="bg-[#242424] text-white">Log in</Button>
      <Button className="bg-white text-[#0a0a0a]">Get Started</Button>

    </div>
  )
}

export default Buttons
